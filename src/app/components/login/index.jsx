
import style from './style.module.css';
import { LoginComp } from './LoginComp';

export const Login = () => {
    return (
        <section className={style.section}>
            <LoginComp />
        </section>
    )
}